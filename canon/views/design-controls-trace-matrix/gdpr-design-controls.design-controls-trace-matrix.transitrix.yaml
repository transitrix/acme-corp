# Report-config view — the audit-facing trace matrix over both chains.
# Spec: notations/views/24-design-controls-trace-matrix.md.
# The document carries no facts of its own: every cell is derived from
# REQUIREMENT / VERIFICATION / HAZARD / RISK_CONTROL in canon, and every gap
# annotation is a rendering of a completeness rule already defined on those
# elements — not a new judgement made here.

notation: design-controls-trace-matrix
spec_version: "0.1"
name: "Acme — GDPR/NIS2 design-controls trace matrix"
generated_at: "2026-07-27"
methodology_version: "2.1.0"

view:
  id: DC_TRACE_MATRIX-ACME-GDPR-1
  name: "Acme — GDPR/NIS2 design-controls trace matrix"
  description: >
    Both chains over Acme's data-protection obligations: which requirements
    have a verification that closed, and which hazards have a control that is
    itself verified. Rows with no closure are annotated with the rule that
    fired, so the gaps are read off the model rather than asserted.
  report_type: combined

  subjects:
    requirements:
      - REQUIREMENT-GDPR-DATA-ERASURE-1
      - REQUIREMENT-GDPR-PORTABILITY-1
      - REQUIREMENT-GDPR-CONSENT-1
      - REQUIREMENT-NIS2-INCIDENT-1
    hazards:
      - HAZARD-ARCHIVE-RETENTION-1
      - HAZARD-CARRIER-PII-EXPOSURE-1

  status_display:
    show_outcomes: ["pass", "fail", "inconclusive", "not_yet_run"]
    show_residual_risk: ["acceptable", "alarp", "unacceptable", "not_recorded"]

  order_rows_by: "id"
